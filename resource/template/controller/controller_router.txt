package {{.entity_name}}_router

import (
	"{{.pkg}}/router"
  {{.entity_name}}_controller "{{.pkg}}/section/{{.entity_name}}/controller"

	"github.com/gin-gonic/gin"
)

func Bind{{.TableName}}Controller (controller *{{.entity_name}}_controller.{{.TableName}}Controller) {
  router.RegisterRouter(func(publicRouterGroup *gin.RouterGroup, authRouterGroup *gin.RouterGroup) {
    publicGroup := publicRouterGroup.Group("/{{.table_name_hyphen}}")
    authGroup := authRouterGroup.Group("/{{.table_name_hyphen}}")

    publicGroup.GET("/hello", controller.Hello)
    authGroup.GET("/hello", controller.Hello)
  })
}